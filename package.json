{
  "name": "sirclo-assignments",
  "version": "1.0.0",
  "main": "fivaa.js",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.0.0",
    "babel-preset-es2015": "^6.24.1",
    "global": "^4.3.2",
    "jest": "^21.2.1",
    "jest": "^21.2.1",
    "jshint": "^2.9.5"
  },
  "babel": {
    "presets": ["es2015"]
  },
  "jest": {
    "testPathIgnorePatterns": [
      "<rootDir>/node_modules/",
      "<rootDir>/src/"
    ],
    "transform": {
      "^.+\\.jsx?$": "babel-jest"
    }
  },
  "scripts": {
    "build": "babel src -d build",
    "test": "jest --verbose --watchAll"
  }
}